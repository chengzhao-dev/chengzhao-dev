# 本机工具配置，随仓库提交。解释器等纯工具路径可以写入；密钥类信息与可识别个人的
# 用户主目录路径仍不得写入。路径用正斜杠（/）避免 TOML 转义。

# Python 解释器，本仓库脚本统一用解释器的完整路径运行；留空按 PATH 查找。
# 换机器或升级环境时改这里，同步更新 .agents/knowledge/local-env/python.md。
python = "D:/ProgramData/miniforge3/python.exe"

# FFmpeg/FFprobe，用于横幅生成；留空按 tool_cache_dir → PATH 顺序查找，
# 或先运行 python .agents/skills/designing-visuals/scripts/media/bootstrap_media.py 下载到缓存目录。
ffmpeg = ""
ffprobe = ""

# 媒体工具缓存目录（.gitignore 已忽略），bootstrap 脚本把下载解压的文件放这里。
tool_cache_dir = ".agents/skills/designing-visuals/scripts/media/tool-cache"
